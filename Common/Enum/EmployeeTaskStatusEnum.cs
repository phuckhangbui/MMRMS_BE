namespace Common.Enum
{
    public enum EmployeeTaskStatusEnum
    {
        Assigned,
        Completed,
        Failed,
        Canceled
    }

    public enum EmployeeTaskTypeEnum
    {
        MaintenanceRequest,
        MaintenaceTicket
    }
}
